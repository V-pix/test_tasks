from setuptools import setup, find_packages


if __name__ == "__main__":
    setup(
        name="tasks",
        version="1.0",
        packages=find_packages(),
        test_suite="test",
        description="tasks",
    )
